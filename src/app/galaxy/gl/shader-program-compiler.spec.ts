import { ShaderProgramCompiler } from './shader-program-compiler';

describe('ShaderProgramCompiler', () => {
  it('should create an instance', () => {
    expect(new ShaderProgramCompiler()).toBeTruthy();
  });
});
